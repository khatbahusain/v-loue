
 <nav class="navbar navbar-expand-md navbar-dark navbar-custom fixed-top">
      
        <a href="<%= root_path %>" class="navbar-brand logo-image"><kbd class="display-5 btn border light p-3">V-loue <i class="fas fa-bicycle fa-2x"></i></kbd></a> 
        
        
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-awesome fas fa-bars"></span>
            <span class="navbar-toggler-awesome fas fa-times"></span>
        </button>
        <!-- end of mobile menu toggle button -->

        <div class="collapse navbar-collapse" id="navbarsExampleDefault">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link page-scroll text-dark" href="#header">HOME <span class="sr-only">(current)</span></a>
                </li>
               

                <li class="nav-item">
                    <a class="nav-link page-scroll text-dark" href="<%= new_rent_path %>">Proposer une location</a>
                </li>






           
                <!-- user not singed in -->         
                <% if user_signed_in? %> 
                <li class="nav-item">
                  <%= link_to "Se déconnecter", destroy_user_session_path, :method => :delete, class: "nav-link page-scroll text-dark" %>
                </li>
                <% else %>
                <li class="nav-item">
                  <%= link_to "Connexion", new_user_session_path, class: "nav-link page-scroll text-dark" %>
                </li>
                 <li class="nav-item">
                  <%= link_to "Inscription", new_user_registration_path, class: "nav-link page-scroll text-dark" %>
                </li>




                
                <% end %>
                <li class="nav-item">
                    <% if current_user %>
                        <% if current_user.avatar.attached? %>
                          <a href="<%= user_path(current_user.id) %>"><%= image_tag current_user.avatar, alt: 'avatar' ,width: '70px', height: '70px', class: "rounded-circle" %><i class="far fa-eye fa-2x text-dark"></i></a>
                        <% else  %>
                           <a href="<%= user_path(current_user.id) %>"><%= image_tag "unkown_user.png", alt: 'avatar' ,width: '70px', height: '70px', class: "rounded-circle"%></a>
                        <% end %>
                      <% end %>
                  </li>
   
            </ul>
          
        </div>

    </nav>
